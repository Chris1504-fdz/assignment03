# Core training + HF stack
torch==2.9.0
transformers==4.57.1
datasets==4.4.0
tokenizers==0.22.1
accelerate==1.11.0
huggingface-hub==0.36.0
safetensors==0.6.2

# Numerics / data (commonly used in eval + metrics)
numpy==2.2.5
pandas==2.3.3
pyarrow==22.0.0
tqdm==4.67.1

# Utility
regex==2025.11.3
tabulate==0.9.0
